{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for document windows.",
  "windows": ["main", "doc-*"],
  "permissions": ["core:default", "dialog:default", "fs:default"]
}
