{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for workbook windows.",
  "windows": ["main", "win-*"],
  "permissions": ["core:default", "dialog:default", "fs:default"]
}
