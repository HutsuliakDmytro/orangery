{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for presentation windows.",
  "windows": ["main", "win-*", "show", "presenter"],
  "permissions": ["core:default", "dialog:default", "fs:default"]
}
