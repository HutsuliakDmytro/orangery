# deps
node_modules/
.pnpm-store/

# build
dist/
dist-ssr/
# One cargo workspace, so the build directory is at the root; the per-app entry
# stays for anyone who builds a crate on its own.
target/
**/src-tauri/target/
**/src-tauri/gen/

# test
coverage/
test-results/
playwright-report/

# corpus runs — the reports are outputs, and `docs/corpus-baseline.md` is the
# part of them worth keeping
corpus-inventory.json
corpus-report*.json
corpus-recalc*.json
corpus-charts/

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.local
.env.*.local
